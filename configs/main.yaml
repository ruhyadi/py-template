# InferenceStack main configurations

# default hydra configuration
defaults:
  - _self_
  - logger: [default]
  - override hydra/hydra_logging: none
  - override hydra/job_logging: none
  
# override hydra output directory
hydra:
  run:
    dir: .
  output_subdir: null

# service mode
service: api